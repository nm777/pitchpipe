{
  "name": "pitchpipe",
  "version": "1.0.3",
  "description": "A web-based pitchpipe application",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "build": "node build.js",
    "deploy": "npm run build && wrangler deploy",
    "release": "npm run version && npm run build && git add . && git commit -m \"release v$(node -p \"require('./package.json').version\")\" && git push"
  },
  "devDependencies": {
    "esbuild": "^0.19.0",
    "wrangler": "^4.51.0"
  }
}
